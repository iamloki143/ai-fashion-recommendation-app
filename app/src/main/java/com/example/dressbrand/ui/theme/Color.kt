package com.example.dressbrand.ui.theme

import androidx.compose.ui.graphics.Color

// === PREMIUM FASHION PALETTE ===

// Core Brand Colors
val DeepObsidian    = Color(0xFF0E0E10)   // Near-black background
val RichCharcoal    = Color(0xFF1A1A1E)   // Card / surface background
val ElevatedSurface = Color(0xFF242428)   // Slightly lifted surface
val SubtleBorder    = Color(0xFF2E2E34)   // Borders / dividers

// Gold Accent System
val LuxeGold        = Color(0xFFD4A84B)   // Primary accent — CTA buttons, highlights
val PaleGold        = Color(0xFFF0D080)   // Text on gold / hover shimmer
val MutedGold       = Color(0xFF8C6E2F)   // Disabled/secondary gold

// Text
val IvoryWhite      = Color(0xFFF5F0E8)   // Primary text
val SilverMist      = Color(0xFFADADB8)   // Secondary / caption text
val DimSteel        = Color(0xFF6B6B7A)   // Placeholder / disabled text

// Semantic
val SoftCrimson     = Color(0xFFB04A4A)   // Error / out-of-stock
val MintSuccess     = Color(0xFF4A8C6A)   // Success / in-stock indicator

// Legacy aliases (keeps old references compiling)
val BabyPink        = ElevatedSurface
val DustyRose       = LuxeGold
val CharcoalPlum    = DeepObsidian
val DustyMauve      = SilverMist
val DeepWine        = MutedGold